<view class="container">

  <view class="topbar">
    <view class="back-btn" bindtap="goBack"><text class="back-arrow">‹</text></view>
    <text class="topbar-title">{{lbl_topbar}}</text>
    <view style="width:60rpx;"></view>
  </view>

  <view class="content">

    <view class="center-block">

      <view class="heading-wrap">
        <text class="heading">{{lbl_heading}}</text>
        <text class="heading-sub">{{lbl_subtitle}}</text>
      </view>

      <!-- Date picker -->
      <picker mode="date" value="{{selectedDate}}" start="{{minDate}}" bindchange="onDateChange">
        <view class="date-display">
          <view class="date-left">
            <text class="date-label">{{lbl_start_label}}</text>
            <text class="date-value">{{selectedDateFormatted}}</text>
          </view>
          <text class="date-arrow">›</text>
        </view>
      </picker>

      <!-- Expiry info -->
      <view class="expiry-card" wx:if="{{expiryDateFormatted}}">
        <text class="expiry-label">{{lbl_plan_ends}}</text>
        <text class="expiry-value">{{expiryDateFormatted}}</text>
        <text class="expiry-sub">{{lbl_5_days}}</text>
      </view>

      <button class="btn-primary" bindtap="goNext">{{lbl_continue}}</button>

    </view>

  </view>


</view>
